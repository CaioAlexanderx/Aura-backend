// AURA. -- storefrontScript.js (shim de compat)
// Codigo real foi movido pra ./storefront/index.js (decomposto em modulos).
// Mantido aqui pra nao quebrar imports legados (`require('./storefrontScript')`).
'use strict';

module.exports = require('./storefront');
